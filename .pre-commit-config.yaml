repos:
  - repo: local
    hooks:
      - id: make-lint
        name: Run make lint as pre-commit hook
        entry: make lint
        language: system
        pass_filenames: false

      - id: make-lint-sql
        name: Run make lint-sql as pre-commit hook
        entry: make lint-sql
        language: system
        pass_filenames: false
